# 開発ルール

このプロジェクトはGitHub上でIssue駆動・PRベースの開発を行う。ClaudeCodeは以下のルールを**例外なく**厳密に守ること。

## 1. Issue駆動

- 実装作業(機能追加・修正・設定変更など)を始める前に、必ず対応するGitHub Issueを作成する(`gh issue create`)。
- Issueには「何を」「なぜ」行うのかを簡潔に記載する。

## 2. ブランチ命名規則

`種別/issue番号-概要` の形式(kebab-case、英語)で作成する。

- `feature/12-add-post-api` — 新機能
- `fix/15-login-bug` — バグ修正
- `chore/20-update-deps` — 雑務・設定変更
- `docs/22-update-readme` — ドキュメント更新

## 3. mainへの直接push禁止

- 常にIssue用のブランチ上で作業する。`main` ブランチへの直接コミット・直接pushは行わない。
- リポジトリ側でも `main` ブランチ保護ルールを設定し、所有者を含め直接pushを技術的に禁止する。

## 4. PRフロー

1. Issue用ブランチを作成
2. 実装
3. `gh pr create` でPRを作成し、本文にIssueをリンクする語句(例: `Closes #12`)を含める
4. PRをマージして完了

単独開発のためレビュー承認は必須ではないが、PRを経由するプロセス自体は必須。

以下のタイミングでは、都度確認を挟まず進めてよい(グローバル設定でcommit・pushそのものは確認不要とされているが、実行タイミングをここで明確化する)。

- ユーザーが実装計画(Plan)を承認したら、そのままIssue用ブランチ作成に進んでよい
- 動作確認(ブラウザでの手動確認など)を経てユーザーから進行の了承を得たら、そのままコミット・pushに進んでよい

## 5. ローカルサーバー起動時のポート固定

- ローカル開発サーバー起動時、対象ポートが既に使用中の場合は**別のポートへフォールバックしたまま起動してはならない**。占有プロセスを停止してから、指定ポート(既定ポート)で改めて起動すること。
- 既定ポートは以下のとおり([基本設計書 1章](docs/basic-design.md#1-技術スタック)の技術スタックに対応)。
  - バックエンド(Spring Boot): `8080`
  - フロントエンド(Vite): `5173`
  - データベース(PostgreSQL): `5432`

## 6. 機密データ・個人情報の取り扱い

- ユーザー名・メールアドレス・パスワード・投稿本文・画像など、アカウントに紐づく実データを扱う機能のため、リポジトリにコミットされるもの(シードデータ、テストフィクスチャ、マイグレーションのサンプル、Issue/PR/READMEに貼るスクリーンショットやログなど)には、実在のメールアドレスや実際のパスワードを一切含めず、必ずダミーデータを使用する。
- パスワードは必ずハッシュ化して保存し、平文でログ・レスポンス・ドキュメントに出さない。
- `.env`・ローカルDBファイル・DBダンプ・クラウドストレージの認証情報(アクセスキー等)は `.gitignore` に必ず含め、誤コミットを防ぐ。

## 関連ドキュメント

- 要件定義: [docs/requirements.md](docs/requirements.md)
- 基本設計: [docs/basic-design.md](docs/basic-design.md)
