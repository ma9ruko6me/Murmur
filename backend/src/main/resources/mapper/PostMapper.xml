<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.murmur.post.PostMapper">

    <resultMap id="postResultMap" type="com.example.murmur.post.Post">
        <id property="id" column="id" />
        <result property="userId" column="user_id" />
        <result property="content" column="content" />
        <result property="createdAt" column="created_at" />
        <result property="updatedAt" column="updated_at" />
        <result property="username" column="username" />
        <result property="displayName" column="display_name" />
        <result property="likeCount" column="like_count" />
        <result property="likedByMe" column="liked_by_me" />
        <result property="commentCount" column="comment_count" />
    </resultMap>

    <sql id="baseSelect">
        SELECT p.id, p.user_id, p.content, p.created_at, p.updated_at,
               u.username, u.display_name,
               COUNT(DISTINCT l.id) AS like_count,
               COALESCE(BOOL_OR(l.user_id = #{currentUserId}), false) AS liked_by_me,
               COUNT(DISTINCT c.id) AS comment_count
        FROM posts p
        JOIN users u ON u.id = p.user_id
        LEFT JOIN likes l ON l.post_id = p.id
        LEFT JOIN comments c ON c.post_id = p.id AND c.deleted_at IS NULL
    </sql>

    <sql id="groupBy">GROUP BY p.id, u.username, u.display_name</sql>

    <select id="findById" resultMap="postResultMap">
        <include refid="baseSelect" /> WHERE p.id = #{id} <include refid="groupBy" />
    </select>

    <select id="findPage" resultMap="postResultMap">
        <include refid="baseSelect" />
        <where>
            <if test="cursorCreatedAt != null and cursorId != null">
                (p.created_at, p.id) &lt; (#{cursorCreatedAt}, #{cursorId})
            </if>
            <if test="authorId != null">
                AND p.user_id = #{authorId}
            </if>
            <if test="authorId == null and scope == 'following' and currentUserId != null">
                AND (p.user_id = #{currentUserId} OR EXISTS (
                    SELECT 1 FROM follows f WHERE f.follower_id = #{currentUserId} AND f.followee_id = p.user_id
                ))
            </if>
        </where>
        <include refid="groupBy" />
        ORDER BY p.created_at DESC, p.id DESC
        LIMIT #{limit}
    </select>

    <select id="countNewerThan" resultType="long">
        SELECT count(*) FROM posts p
        <where>
            p.id > #{afterId}
            <if test="scope == 'following' and currentUserId != null">
                AND (p.user_id = #{currentUserId} OR EXISTS (
                    SELECT 1 FROM follows f WHERE f.follower_id = #{currentUserId} AND f.followee_id = p.user_id
                ))
            </if>
        </where>
    </select>

    <insert id="insert" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO posts (user_id, content) VALUES (#{userId}, #{content})
    </insert>

    <update id="update">
        UPDATE posts SET content = #{content}, updated_at = now()
        WHERE id = #{id}
    </update>

    <delete id="delete">
        DELETE FROM posts WHERE id = #{id} AND user_id = #{userId}
    </delete>

</mapper>
